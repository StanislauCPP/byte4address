Testing byte4address type. But all these tests work with postgres.
Before use these tests, need to build and install byte4address.
Every time when byte4address library changed we need to build and install byte4address and after we can use these tests.